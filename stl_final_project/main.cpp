#include <iostream>
#include <vector>
#include <map>
#include <algorithm>
std::vector<int> numVector;

int main()
{
    for(int i = 1; i <1001; ++i)
    {
        numVector.push_back(i);
    }
    auto vec_it = numVector.begin();

    return 0;
}
